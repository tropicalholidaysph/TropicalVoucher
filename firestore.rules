rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Tropical Ledger Security Rules - MVP Permissive Mode
    // Allows any authenticated user (including anonymous) full access to all data.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}