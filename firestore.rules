rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Highly permissive rules for an authenticated MVP environment
    // This allows any logged-in user (including anonymous) to read and write all data.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}